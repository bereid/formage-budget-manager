import React from 'react';
import { ScrollPanel } from 'primereact/scrollpanel';

const ScrollWidget = () => (
  <ScrollPanel style={{ width: '100%', height: '200px', backgroundColor: 'red' }}>
  </ScrollPanel >
);

export default ScrollWidget;